export type GetAce = {
  id: string;
};

export type CreateAce = {
  name: string;
  age: number;
  email: string;
};

export type UpdateAce = {
  id: string;
  name: string;
  age: number;
  email: string;
};
